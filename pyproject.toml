[project]
name = "smallllm"
version = "0.0.1"
description = "samll scale parameter to learn the modern Language model"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "torch>=2.11.0",
]

[[tool.uv.index]]
url = "https://mirrors.aliyun.com/pypi/simple"
default = true
